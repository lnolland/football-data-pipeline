# Football Data Pipeline

Pipeline d’ingestion de données football depuis une API publique vers PostgreSQL.
Objectif : montrer les bases **DevOps (Docker/CI/CD/K8s)** et **Data Engineering (ingestion, schéma, idempotence)**.

## Run local
1) Ajouter le token.
2) Lancer :
```bash
docker compose up --build
